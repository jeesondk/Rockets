info:
  contact: {}
paths:
  /messages:
    post:
      description: returns string
      produces:
      - text/plain
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Receive Rocket status messages
      tags:
      - Message Service
swagger: "2.0"
